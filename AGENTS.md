# Pinu Game 开发流程

后续在 `Pinu_game/` 内做开发时，遵循这个流程。

## 1. 开始前

- 先说明目标、假设和不确定点。
- 如果需求有多种解释，先列出来并确认。
- 如果存在更简单的实现方式，优先选择简单方式并说明取舍。

## 2. 实现原则

- 只实现本次明确要求的内容。
- 不为单次使用代码提前抽象。
- 不增加未要求的配置、扩展能力或额外功能。
- 只修改和本次目标直接相关的文件。
- 匹配项目已有风格，不顺手重构无关代码。

## 3. 验证方式

每次任务都要写清成功标准，例如：

```text
1. 创建/修改目标文件 -> verify: 文件存在且内容符合需求
2. 实现功能 -> verify: 本地运行或打开页面确认可用
3. 准备提交 -> verify: git status 只包含本次相关改动
```

## 4. Git 提交流程

- 使用与任务一致的分支名。
- 提交前检查 `git status --short`。
- commit message 使用清晰的动词开头。
- PR 描述包含 `Summary`、`Description` 和 `Verification`。
- 不提交 `.DS_Store`、日志、依赖目录或临时文件。

## 5. 默认边界

默认只做最小可验证改动。除非需求明确要求，不展开具体玩法实现、不新增复杂工程配置、不改动 `Pinu_game/` 之外的文件。

## 6. 产品文档协作补充

涉及 Bingo Challenge 或小游戏方向时，先阅读：

1. `docs/README.md`
2. `docs/pinu-bingo-decisions-and-collaboration.md`
3. `docs/bingo-challenge-requirements.md`

沟通和产出要求：

- 全程使用中文，默认站在产品经理视角解释，但要同时考虑 UI、教研和研发实现。
- 改需求文档前，先指出可能的定位冲突、规则冲突、MVP 边界和待决策项。
- 新增玩法规则时，同时补齐产品目的、用户体验、教研边界、UI 表达、研发实现和数据验证口径。
- 流程图或视觉稿交付时，尽量同时说明源文件、导出文件和建议插入位置。
- 不确定的问题不要直接替产品拍板，先说明假设和风险，再推动确认。

我是产品经理，尽可能保持产品思维与我沟通，但方案要经得起我的老板产品总监验证、也要考虑ui、教研、研发部门的观点和实现方式。
