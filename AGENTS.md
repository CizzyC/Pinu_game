# Pinu Game 开发流程

后续在 `Pinu_game/` 内做开发时，遵循这个流程。

## 1. 开始前

- 先说明目标、假设和不确定点。
- 如果需求有多种解释，先列出来并确认。
- 如果存在更简单的实现方式，优先选择简单方式并说明取舍。

## 2. 实现原则

- 只实现本次明确要求的内容。
- 不为单次使用代码提前抽象。
- 不增加未要求的配置、扩展能力或额外功能。
- 只修改和本次目标直接相关的文件。
- 匹配项目已有风格，不顺手重构无关代码。

## 3. 验证方式

每次任务都要写清成功标准，例如：

```text
1. 创建/修改目标文件 -> verify: 文件存在且内容符合需求
2. 实现功能 -> verify: 本地运行或打开页面确认可用
3. 准备提交 -> verify: git status 只包含本次相关改动
```

## 4. Git 提交流程

- 使用与任务一致的分支名。
- 提交前检查 `git status --short`。
- commit message 使用清晰的动词开头。
- PR 描述包含 `Summary`、`Description` 和 `Verification`。
- 不提交 `.DS_Store`、日志、依赖目录或临时文件。

## 5. 默认边界

默认只做最小可验证改动。除非需求明确要求，不展开具体玩法实现、不新增复杂工程配置、不改动 `Pinu_game/` 之外的文件。
